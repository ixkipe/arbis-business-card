/**
 * 
 * @param {[{picture: string, text: string}]} projects
 */
function initializeCarousel(projects) {
  let carousel = document.querySelector('#carouselMainContainer');

  projects.forEach(project => {
    
  });
}